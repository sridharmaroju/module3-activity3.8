apiVersion: v1
kind: Service
metadata:
  name: sridhar-clusterip-service
  namespace: sridhar-eks-activity
spec:
  type: ClusterIP
  selector:
    app: httpd-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 5678