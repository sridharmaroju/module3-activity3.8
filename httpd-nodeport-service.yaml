apiVersion: v1
kind: Service
metadata:
  name: sridhar-nodeport-service
  namespace: sridhar-eks-activity
spec:
  type: NodePort
  selector:
    app: httpd-app
  ports:
    - protocol: TCP
      port: 30001
      targetPort: 5678