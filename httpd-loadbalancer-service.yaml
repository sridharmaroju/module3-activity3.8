apiVersion: v1
kind: Service
metadata:
  name: sridhar-loadbalancer-service
  namespace: sridhar-eks-activity
spec:
  type: LoadBalancer
  selector:
    app: httpd-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5678